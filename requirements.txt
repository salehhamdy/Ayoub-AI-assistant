# ── Core ──────────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
colorama>=0.4.6
rich>=13.0.0

# ── LLM providers ─────────────────────────────────────────────────────────────
google-genai>=1.0.0                   # New unified Google SDK (from google import genai)
google-generativeai>=0.8.0            # Legacy SDK — used by vision multimodal calls
langchain>=1.0.0
langchain-core>=1.0.0
langchain-community>=0.4.0
langchain-experimental>=0.4.0
langchain-openai>=1.0.0
langchain-groq>=1.0.0
langchain-google-genai>=4.0.0
openai>=1.0.0
groq>=0.10.0
ollama>=0.5.0

# ── Search ────────────────────────────────────────────────────────────────────
ddgs>=9.0.0                           # DuckDuckGo search API (renamed from duckduckgo-search)
beautifulsoup4>=4.12.0
lxml>=5.0.0
requests>=2.31.0

# ── MCP server ────────────────────────────────────────────────────────────────
fastmcp>=2.0.0
httpx>=0.27.0
mcp>=1.0.0

# ── Voice / LiveKit ──────────────────────────────────────────────────────────
# STT + LLM : Groq (free — uses GROQ_API_KEY)
# TTS       : Cartesia (free tier — https://play.cartesia.ai)
# VAD       : Silero (local, no key needed)
livekit-agents[silero]>=1.5.0
livekit-plugins-groq>=1.5.0
livekit-plugins-cartesia>=1.5.0

# ── File tools ────────────────────────────────────────────────────────────────
PyPDF2>=3.0.0

# ── Vision / screen capture ───────────────────────────────────────────────────
pillow>=10.0.0
opencv-python>=4.8.0

# ── Image generation ──────────────────────────────────────────────────────────
gradio_client>=1.0.0
matplotlib>=3.7.0
